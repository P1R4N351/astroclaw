apiVersion: v1
kind: Service
metadata:
  name: astroclaw
  labels:
    app: astroclaw
spec:
  type: ClusterIP
  selector:
    app: astroclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
