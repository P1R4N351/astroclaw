apiVersion: v1
kind: ConfigMap
metadata:
  name: astroclaw-config
  labels:
    app: astroclaw
data:
  astroclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.astroclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Astroclaw Assistant",
            "workspace": "~/.astroclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Astroclaw Assistant

    You are a helpful AI assistant running in Kubernetes.
