interface:
  display_name: "Astroclaw Release CI"
  short_description: "Verify and debug Astroclaw release validation runs"
  default_prompt: "Use $astroclaw-release-ci to preflight provider secrets, watch full release validation, summarize child runs, and triage only failing release lanes."
