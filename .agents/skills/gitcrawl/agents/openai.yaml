interface:
  display_name: "Gitcrawl"
  short_description: "Search local Astroclaw issue and PR history before live GitHub triage"
  default_prompt: "Use $gitcrawl to inspect Astroclaw issue and PR history, find related threads and duplicate candidates, then verify actionable decisions with live GitHub."
